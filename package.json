{
  "name": "glen-engine",
  "version": "0.0.1",
  "description": "Moteur 3D web minimal (WebGPU, architecture GameObject/Component) — sans dépendance runtime.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.71",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
